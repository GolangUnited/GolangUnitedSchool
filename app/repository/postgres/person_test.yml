conf:
  setup:
    requests:
      - request: CREATE
          TABLE person (
          "id" serial NOT NULL,
          "first_name" VARCHAR(255) NOT NULL,
          "last_name" VARCHAR(255) NOT NULL,
          "patronymic" VARCHAR(255) NULL,
          "login" VARCHAR(255) NOT NULL unique ,
          "role_id" integer NOT NULL,
          "passwd" VARCHAR(255) NOT NULL,
          "updated_at" TIMESTAMP NOT NULL default current_timestamp,
          "deleted" BOOLEAN NOT NULL default false,
          "created_at" TIMESTAMP NOT NULL default current_timestamp,
          "email" VARCHAR(255) NOT NULL default '',
          "birthday" VARCHAR(255) NOT NULL default '',
          CONSTRAINT "person_pk" PRIMARY KEY ("id")
          );
      - request: INSERT
          INTO person
          (first_name, last_name, patronymic, login, role_id, passwd, deleted, updated_at) VALUES
          ('1', '1', '1', '1', 1, '1', 'false', '2006-01-02'),
          ('2', '2', '2', '2', 2, '2', 'false', '2006-01-02'),
          ('3', '3', '3', '3', 3, '3', 'false', '2006-01-02');
  teardown:
    requests:
      - request: DROP TABLE person CASCADE;